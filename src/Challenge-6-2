import java.util.Scanner;

public class Sandbox {
    public static void main(String[] args) {
        Scanner baca = new Scanner(System.in);
        Integer angka = baca.nextInt();
        baca.close();

        for (int i = 1; i <= angka; i++) {
            if (angka % i == 0) {
                System.out.println(i);
            }
        }
    }
}